git status --> prints the status of the git repo
git init --> initializes the Git Repository (let's just update again and see that git diff works)
git config --global user.name "tienle488"
git config --global user.email tienle_355@msn.com
git config user.name "tienle488"
git config user.email tienle_355@msn.com
git add
git commit -m ""
git log
git diff <filename>